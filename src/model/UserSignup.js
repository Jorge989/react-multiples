class UserSignup {
  constructor(username, email, password, confirmPassword, id) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.confirmPassword = confirmPassword;
    this.id = id;
  }
}
export default UserSignup;
